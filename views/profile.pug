extends layout 

block append head 
    link(rel='stylesheet', href='/stylesheets/profile.css')

block content 
    .profileContainer
        // Yeah I mixed up left and right... no i wont change it...
        .profileRight 
            img(src="/images/blank-pfp.jpg")

            a(href="/profile/edit")
                button(class="editProfile") Edit Profile

            a(href="")   
                button(class="logout") Log Out
            
        .profileMiddle
            if(user.name)
                h1 #{user.name}
            else
                h1 #{user.email}

            p #{user.description}

        .profileLeft
            h2 Your Groups 

            a.bandLink(href="#")
                img(src="/images/blank-pfp.jpg")
                p Radiohead

            a.bandLink(href="#")
                img(src="/images/blank-pfp.jpg")
                p The Smile 

            a.bandLink(href="#")
                img(src="/images/blank-pfp.jpg")
                p Atoms For Peace



                
                


            